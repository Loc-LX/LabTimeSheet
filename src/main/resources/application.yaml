spring:
  application:
    name: labtimesheet
  profiles:
    default: dev
    group:
      e2e: dev
  docker:
    compose:
      enabled: false
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

management:
  endpoint:
    health:
      probes:
        enabled: true
